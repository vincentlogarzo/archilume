# Push Skill
1. Run `git status` to see all changes
2. Show the user a summary of modified files
3. Stage all changes with `git add -A`
4. Generate a concise commit message based on the diff
5. Commit and push to the current branch
6. Confirm success with the commit hash
