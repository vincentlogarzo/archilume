__pycache__/
*.egg-info/

# Claude Code settings and credentials
.claude/
.env
.env.local
*.key

# Ignore all content in these directories except .gitkeep files
inputs/*
!inputs/.gitkeep

# For outputs: ignore contents but preserve directory structure with .gitkeep
# The order matters: negations must come AFTER the ignore patterns
outputs/*
!outputs/.gitkeep
!outputs/*/
outputs/*/*
!outputs/*/.gitkeep

# Ignore archive contents but preserve the directory via .gitkeep
archive/*
!archive/.gitkeep
!archive/.gitignore

# SSH configuration (contains private keys - keep these secret!)
.ssh/
!.ssh/config.template